The root library directory for the application.

Dev Notes:

Divide up the pieces into different modules.  For example, base functionality
like generating random numbers is in a base module, the individual generators
are in another, error determination in another, I/O and interface in another.